sudo apt install postgresql
sudo service postgresql start
# Accessing PostgreSQL:

# By default, PostgreSQL creates a user called postgres. You can access the PostgreSQL prompt by switching to the postgres user and running the psql command:
css

sudo -i -u postgres
psql

# You can also create additional users and grant them privileges as needed.

# Creating a new User

# CREATE ROLE new_user WITH LOGIN PASSWORD 'password';

# GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO new_user;

pip install psycopg2-binary Flask-SQLAlchemy

